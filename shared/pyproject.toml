[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "radassist-shared"
version = "0.1.0"

[tool.setuptools]
packages = ["shared", "shared.embeddings", "shared.phi_masking"]

[tool.setuptools.package-dir]
shared = "."
