# Phase 0 — steps 1 & 2 (label building + splitting), CPU-only, no downloads
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pyyaml>=6.0

# Phase 0 — step 3 (encoder bake-off), needs GPU + model downloads
torch>=2.1
torchvision>=0.16
open_clip_torch>=2.24
pillow>=10.0

# Used by the production system (not required for the Phase 0 eval, which uses
# exact numpy kNN for metric correctness). Needed starting Phase 3 (retrieval
# indexing) since ml/retrieval/build_chroma_index.py writes into it directly:
chromadb>=0.4

# Phase 4 — backend/tests/ (unit + integration)
pytest>=7.4

# Phase 4 Step 9 — database layer (SQLAlchemy 2.0 typed ORM, SQLite for now;
# pydantic-settings was already a transitive dep via chromadb, now direct)
sqlalchemy>=2.0
pydantic-settings>=2.0

# Phase 4 Step 10 — Alembic migrations
alembic>=1.13

# Phase 4 Step 11 — FastAPI skeleton (httpx was already a transitive dep
# via chromadb; fastapi/uvicorn/python-multipart are now direct -- uvicorn
# is what actually serves this app, not something that should depend on
# chromadb happening to still pull it in)
fastapi>=0.110
uvicorn>=0.30
python-multipart>=0.0.9

# shared/ and backend/ are editable-installed as local packages so
# `from shared.embeddings...` and `from app...` resolve regardless of the
# process's CWD -- replaces the sys.path hacks/conftest.py shim used
# through Step 9. Run from repo root: pip install -r requirements.txt
-e ./shared
-e ./backend
